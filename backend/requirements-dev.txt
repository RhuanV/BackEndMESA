# Development-only tools (not installed into the production image).
# Install with: pip install -r requirements.txt -r requirements-dev.txt

# Git hooks for code quality
pre-commit==4.6.0

# Standardized, semantic commit messages
commitizen==4.16.4

# Test runner
pytest==9.1.1
