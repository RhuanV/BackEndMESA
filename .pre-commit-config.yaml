repos:
  - repo: https://github.com/compilerla/conventional-pre-commit
    rev: v3.2.0  # Use the most recent version
    hooks:
      - id: conventional-pre-commit
        stages: [commit-msg]
        args: [] # Here, we can customize any type we want to